<%= form_for(@offline_user) do |f| %>
  <% if @offline_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@offline_user.errors.count, "error") %> prohibited this offline_user 
        from being saved:</h2>

      <ul>
      <% @offline_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :contactno %><br>
    <%= f.number_field :contactno %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
